{
  "name": "send-discord-message",
  "version": "1.0.0",
  "description": "GitHub Action to send messages to Discord",
  "main": ".github/actions/discord_send_message/discord_send_message.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^3.4.1",
    "prettier": "^3.3.3"
  }
}
